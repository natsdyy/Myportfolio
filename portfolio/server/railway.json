{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && rm -rf ~/.npm /tmp/npm-* node_modules/.cache 2>/dev/null || true && npm install --no-optional --no-audit --no-fund && npm run build && cd server"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

