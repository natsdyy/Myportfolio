{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && rm -rf node_modules/.cache ~/.npm/_cacache ~/.npm/_logs ~/.npm/_locks 2>/dev/null || true && npm cache clean --force 2>/dev/null || true && npm install --legacy-peer-deps --no-audit && npm run build && cd server"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

